# Screenshots

Replace these placeholder files with actual screenshots of the running Vue admin panel.

## Required screenshots

- `dashboard.png` — Dashboard with KPI cards and trend chart
- `materials.png` — Materials page (keywords tab)
- `prompts.png` — Prompt library cards
- `models.png` — Model configuration
- `tasks.png` — Task scheduler
- `articles.png` — Article management table
- `publications.png` — Publication center
- `capabilities.png` — Capability library

## How to capture

1. Start the app: `docker compose up -d`
2. Open `http://localhost:5173` (dev) or `http://localhost:8001/console/` (prod)
3. Navigate to each page and take a full-window screenshot
4. Save as PNG to this directory
